import * as React from 'react';
import { StyleSheet, View } from 'react-native';

import { Text } from '@/ui';

const ExtraOffer = () => {
  return (
    <View style={styles.container}>
      <Text>Ưu đãi thêm</Text>
    </View>
  );
};

export default ExtraOffer;

const styles = StyleSheet.create({
  container: {},
});
